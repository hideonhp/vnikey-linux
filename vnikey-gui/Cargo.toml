[package]
name = "vnikey-gui"
version = "0.2.1"
edition = "2024"

[dependencies]
eframe = "0.35.0"
tokio = { version = "1.0", features = ["rt", "rt-multi-thread", "macros"] }
serde = { version = "1.0.229", features = ["derive"] }
vnikey-config = { version = "0.2.1", path = "../vnikey-config" }
