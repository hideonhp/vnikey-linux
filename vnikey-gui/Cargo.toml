[package]
name = "vnikey-gui"
version = "0.1.2"
edition = "2024"

[dependencies]
eframe = "0.35.0"
serde = { version = "1.0.229", features = ["derive"] }
vnikey-config = { version = "0.1.2", path = "../vnikey-config" }
